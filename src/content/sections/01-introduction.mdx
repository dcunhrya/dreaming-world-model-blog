---
title: "Introduction"
order: 1
description: "Introduction to the dreaming blog."
---

## Introduction

This is a placeholder section for your latest research blog. Replace this content with your article prose in `src/content/sections/`.

Add new sections by creating additional MDX files in `src/content/sections/` with frontmatter:

```yaml
title: "Section title"
order: 2
description: "Optional section description."
```

<Callout type="note" title="Getting started">
Run `npm install` and `npm run dev` from the `dreaming` folder to preview locally.
Add React components under `src/components/` when you need interactive visuals.
</Callout>
